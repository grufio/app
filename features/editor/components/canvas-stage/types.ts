/**
 * Shared canvas-stage types.
 *
 * Responsibilities:
 * - Define small geometry/state shapes shared across helpers/controllers.
 */
export type ViewState = { scale: number; x: number; y: number }

export type BoundsRect = { x: number; y: number; w: number; h: number }

