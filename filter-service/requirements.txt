fastapi==0.115.5
uvicorn[standard]==0.32.1
pillow==11.0.0
python-multipart==0.0.20
opencv-python-headless==4.10.0.84
numpy==2.2.2
# F20: vtracer-driven numerate (replaces the rect-string loop in
# /filters/numerate). vtracer collapses adjacent same-color cells
# into one polygon per connected component; lxml parses + walks the
# resulting SVG. shapely computes per-region centroids for the
# upcoming paint-by-numbers label placement.
vtracer==0.6.15
lxml==5.4.0
shapely==2.0.7
