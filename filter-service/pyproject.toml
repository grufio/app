[tool.pytest.ini_options]
testpaths = ["tests"]
# Make `from app.main import app` resolve without an installed package.
pythonpath = ["."]
addopts = "-q"

[tool.coverage.run]
source = ["app"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
