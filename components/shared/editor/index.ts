/**
 * Shared editor component exports (barrel).
 *
 * Responsibilities:
 * - Legacy shim: re-export editor components from the feature boundary.
 *
 * Note:
 * - New canonical location is `features/editor/**`.
 */
export * from "@/features/editor"

